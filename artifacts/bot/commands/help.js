const { container, text, separator, v2 } = require('../utils/components');

const data = {
  name: 'help',
  description: 'Список всех команд бота',
};

async function execute(client, interaction) {
  await interaction.reply({
    ...v2([
      container([
        text(
          `## 📋 Список команд\n\n` +

          `**Модерация** *(префикс \`?\`)*\n` +
          `\`?mute @user [время] [причина]\` — замутить участника\n` +
          `\`?unmute @user / #номер_кейса\` — снять мут\n` +
          `\`?ban @user [причина]\` — забанить участника\n` +
          `\`?unban @user / #номер_кейса\` — разбанить\n` +
          `\`?clear [кол-во/@user] [время]\` — очистить сообщения\n`
        ),
        separator(),
        text(
          `**Активность и рейтинг**\n` +
          `\`?rank [@user]\` — профиль, уровень, XP и войс-активность\n` +
          `\`?rep @user\` — выдать респект\n` +
          `\`?lb\` — таблица лидеров по опыту\n`
        ),
        separator(),
        text(
          `**Слэш-команды**\n` +
          `\`/открытка\` — отправить открытку пользователю\n` +
          `\`/birthday дата\` — установить день рождения (ДД.ММ)\n` +
          `\`/giveaway\` — создать розыгрыш *(только организаторы)*\n`
        ),
        separator(),
        text(
          `**Только <@985202067041845258>**\n` +
          `\`?exp @user [кол-во]\` — выдать опыт\n` +
          `\`?lvl @user [+/-кол-во]\` — изменить уровень\n` +
          `\`?2x\` — включить двойной опыт\n` +
          `\`?2xoff\` — завершить двойной опыт`
        ),
      ]),
    ], true),
  });
}

module.exports = { data, execute };
